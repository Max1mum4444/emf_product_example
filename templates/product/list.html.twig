{% extends 'base.html.twig' %}

{% block main %}

    {% for product in products %}
        <h3>{{ product.title }}</h3>

        <pre>
            {{ dump(product) }}
        </pre>
    {% endfor %}
{% endblock %}
